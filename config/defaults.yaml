parameters:
  app.datasources.provider: default
  app.custom-links: [ ]
  app.save:
    webhook-url: null
    webhook-timeout: 10
  app.notification:
    webhook-url: null
  app.all-meals:
    remove-pattern: [ ]
    exclude-pattern: [ ]