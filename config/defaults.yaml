parameters:
  app.datasources.provider: default
  app.max-days: 30
  app.custom-links: [ ]
  app.grouped-meals.exclude-patterns: [ ]
  app.grouped-meals.remove-patterns: [ ]
  app.save:
    webhook-url: null
    webhook-timeout: 10
  app.notification:
    webhook-url: null